import UIKit

class EpisodesViewController: UIViewController {

    var episodesData: [DataClassEpisode] = []
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        //print("\n\(i.id) : \(i.airDate) : \(i.title) : \(i.datumDescription): \(i.director.name) : \(i.writer.name)")
        
        /*
         Episodios tienen:
            id
            fecha de salida
            descripcion
            director
            escritor
         */
    }
    
}
